{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Run gym-hpa script",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}",
        "MPLBACKEND": "Agg"
      },
      "args": [
        "--testing"
      ]
    }
  ]
}
